import { createContext } from "react";

// Creamos el contexto
// Se utiliza para compartir información entre componentes sin pasar props (parámetros cómo variables, states, funciones, etc.)
export const ThemeContext = createContext();
